import Skeleton from "../layout/skeleton";

interface UltimateEntityCardSkeletonProps {}

const UltimateEntityCardSkeleton = ({}: UltimateEntityCardSkeletonProps) => {
  return <Skeleton className="w-full h-32" />;
};

export default UltimateEntityCardSkeleton;
